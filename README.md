# Auth Service - Микросервис аутентификации

## Описание проекта

**Auth Service** - это современный микросервис аутентификации, построенный на FastAPI, который предоставляет полнофункциональную систему регистрации, входа и управления пользователями. Сервис включает в себя JWT-аутентификацию с ролевой моделью доступа, интеграцию с очередью сообщений RabbitMQ и API Gateway для маршрутизации запросов.

> ⚠️ **Важно**: Проект находится в стадии разработки и содержит хардкод и известные ограничения, которые планируется устранить в будущих версиях.

## 🏗️ Архитектура

Проект построен по микросервисной архитектуре и состоит из трех основных компонентов:

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   API Gateway   │    │  Auth Service   │    │   RabbitMQ      │
│   (KrakenD)     │────│   (FastAPI)     │────│   Message Queue │
│   Port: 8080    │    │   Port: 8000    │    │   Port: 5672    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### Компоненты системы:

1. **API Gateway (KrakenD)** - точка входа для всех клиентских запросов
2. **Auth Service** - основной сервис аутентификации на FastAPI с ролевой моделью
3. **RabbitMQ** - брокер сообщений для асинхронной обработки событий
4. **SQLite** - база данных для хранения информации о пользователях

## 🛠️ Технологии

### Backend
- **Python 3.11** - современная версия Python
- **FastAPI** - высокопроизводительный веб-фреймворк
- **SQLAlchemy (Async)** - асинхронная ORM для работы с БД
- **AIOSQLite** - асинхронный драйвер для SQLite
- **JWT (jose)** - библиотека для работы с JSON Web Tokens
- **Passlib[Argon2]** - безопасное хеширование паролей
- **AIO-Pika** - асинхронный клиент для RabbitMQ

### Infrastructure
- **Docker & Docker Compose** - контейнеризация и оркестрация
- **KrakenD** - высокопроизводительный API Gateway
- **RabbitMQ 3** - брокер сообщений с веб-интерфейсом

### Дополнительно
- **Pydantic** - валидация данных и схемы API
- **Email-Validator** - валидация email адресов

## 📁 Структура проекта

```
auth-service/
├── docker-compose.yml          # Конфигурация Docker Compose
├── README.md                   # Документация проекта
├── auth-service/
│   ├── Dockerfile             # Dockerfile для сервиса аутентификации
│   ├── requirements.txt       # Python зависимости
│   └── app/
│       ├── main.py            # Главный файл FastAPI приложения
│       ├── models.py          # SQLAlchemy модели данных
│       ├── schemas.py         # Pydantic схемы для валидации
│       ├── crud.py            # Операции с базой данных
│       ├── auth.py            # Функции хеширования паролей
│       ├── jwt.py             # JWT токены и аутентификация
│       ├── database.py        # Конфигурация базы данных
│       ├── sender.py          # Отправка сообщений в RabbitMQ
│       └── validators.py      # Вспомогательные валидаторы
└── gateway/
    └── krakend.json           # Конфигурация KrakenD Gateway
```

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd auth-service
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Проверьте статус сервисов:
```bash
docker-compose ps
```

> 💡 **Healthcheck**: RabbitMQ настроен с healthcheck, который проверяет доступность каждые 30 секунд.

### Локальная разработка

1. Установите зависимости:
```bash
cd auth-service
pip install -r requirements.txt
```

2. Запустите RabbitMQ (опционально для локальной разработки):
```bash
docker run -d -p 5672:5672 -p 15672:15672 rabbitmq:3-management
```

3. Запустите сервис аутентификации:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 🔌 API Endpoints

### Через API Gateway (порт 8080)

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/register` | Регистрация нового пользователя | ❌ |
| POST | `/api/login` | Вход в систему | ❌ |
| POST | `/api/refresh` | Обновление токенов | ❌ |
| POST | `/api/account-delete` | Деактивация своего аккаунта | ✅ (Bearer token) |

### Прямое обращение к Auth Service (порт 8000)

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/register` | Регистрация нового пользователя | ❌ |
| POST | `/login` | Вход в систему | ❌ |
| POST | `/refresh` | Обновление токенов | ❌ |
| POST | `/account-delete` | Деактивация своего аккаунта | ✅ (Bearer token) |
| POST | `/account-delete/{user_id}` | Деактивация чужого аккаунта (Admin) | ✅ (Bearer token + role: admin) |

> ⚠️ **Важно**: Эндпоинт `/account-delete/{user_id}` требует роль 'admin' и доступен только напрямую через Auth Service (не через Gateway).

## 📋 Схемы данных

### RegisterSchema
```json
{
  "display_name": "string (3-30 символов, только буквы)",
  "login": "string (3-30 символов, буквы/цифры/_)",
  "email": "string (валидный email)",
  "password": "string (мин. 8 символов, должен содержать строчные и заглавные буквы, цифры и спецсимволы)"
}
```

### LoginSchema
```json
{
  "login": "string",
  "password": "string"
}
```

### AuthResponseSchema
```json
{
  "user": {
    "id": "integer",
    "login": "string",
    "email": "string",
    "is_active": "boolean"
  },
  "token_pair": {
    "access_token": "string (действует 30 минут)",
    "refresh_token": "string (действует 7 дней)"
  }
}
```

### RefreshTokenSchema
```json
{
  "refresh_token": "string"
}
```

### TokenResponseSchema
```json
{
  "access_token": "string",
  "refresh_token": "string"
}
```

### AuthInfo
```json
{
  "id": "integer",
  "login": "string",
  "email": "string",
  "is_active": "boolean"
}
```

## 🔒 Безопасность и Авторизация

### Ролевая модель доступа

Сервис поддерживает ролевую модель доступа с помощью JWT токенов:

- **role**: поле в payload JWT токена
- **required_role()**: зависимость для проверки прав доступа
- **Текущая реализация**: роль 'user' по умолчанию для всех пользователей

> ⚠️ **Ограничение**: Роль захардкожена как 'user' при создании токенов. Отсутствует механизм назначения ролей пользователям в базе данных.

### JWT Токены

#### Access Token
- **Время жизни**: 30 минут
- **Содержит**: `sub` (user_id), `exp` (expiration), `role`, `type: 'access'`
- **Используется для**: доступа к защищенным эндпоинтам

#### Refresh Token
- **Время жизни**: 7 дней
- **Содержит**: `sub` (user_id), `exp` (expiration), `role`, `type: 'refresh'`, `jti` (token ID), `iat` (issued at)
- **Используется для**: обновления пары токенов
- **Ротация**: при обновлении создается новая пара токенов (весь refresh токен заменяется)

> ⚠️ **Ограничение**: Отсутствует черный список (blacklist) для отозванных токенов. Refresh токены могут использоваться до истечения срока действия.

### Аутентификация и авторизация

- **JWT токены** с алгоритмом HS256
- **Access Token** - действует 30 минут
- **Refresh Token** - действует 7 дней с полной ротацией
- **Хеширование паролей** с использованием Argon2
- **Валидация данных** через Pydantic схемы

### Валидация данных

- **Пароль**: минимум 8 символов, должен содержать заглавные и строчные буквы, цифры и спецсимволы (`!@#$%^&*()-=_`)
- **Логин**: 3-30 символов, только буквы, цифры и подчеркивания
- **Имя пользователя**: 3-30 символов, только буквы
- **Email**: обязательная валидация формата

### Защита от атак

- Защита от SQL-инъекций через ORM
- Валидация всех входных данных через регулярные выражения
- Хеширование паролей с солью (Argon2)
- Временные ограничения на токены
- Проверка на активные аккаунты при регистрации

## 🚨 Известные ограничения и хардкод

### Хардкод в коде

1. **Секретный ключ JWT** (`auth-service/app/jwt.py`):
   ```python
   SECRET = 'super-secret'  # ⚠️ Должен быть в переменных окружения
   ```

2. **Роль пользователя** (`auth-service/app/jwt.py`):
   ```python
   payload = {
       'role': 'user',  # ⚠️ Захардкожено, нет механизма назначения ролей
   }
   ```

3. **Роль администратора** (`auth-service/app/main.py`):
   ```python
   admin = Depends(required_role('admin'))  # ⚠️ Роль захардкожена
   ```

4. **URL подключения к RabbitMQ** (`auth-service/app/sender.py`):
   ```python
   connection = await connect_robust("amqp://guest:guest@rabbitmq:5672/")  # ⚠️ Захардкожен
   ```

5. **Алгоритм JWT** (`auth-service/app/jwt.py`):
   ```python
   ALGORITHM = 'HS256'  # ⚠️ Может быть вынесен в конфиг
   ```

### Архитектурные ограничения

1. **Ролевая система**:
   - Отсутствует механизм назначения ролей пользователям
   - Роль захардкожена как 'user' при создании токенов
   - Проверка роли работает только для одной роли (не поддерживает список ролей)
   - Нет таблицы ролей в базе данных

2. **Управление токенами**:
   - Отсутствует черный список для отозванных токенов
   - Нет механизма инвалидации всех токенов пользователя
   - Refresh токены могут использоваться до истечения срока действия
   - Отсутствует лимит на количество активных сессий

3. **База данных**:
   - SQLite не подходит для продакшн с высокой нагрузкой
   - Отсутствует механизм миграций (Alembic)
   - Нет индексов для оптимизации запросов

4. **Обработка ошибок**:
   - Часть ошибок возвращает статус 401 вместо более подходящих кодов
   - Отсутствует централизованная обработка исключений
   - Нет детального логирования ошибок

5. **Конфигурация**:
   - Отсутствует файл конфигурации (config.py / .env)
   - Все настройки захардкожены в коде
   - Нет разделения конфигураций для dev/prod

6. **Безопасность**:
   - Отсутствует rate limiting
   - Нет защиты от brute-force атак
   - Отсутствует логирование неудачных попыток входа
   - Нет CAPTCHA для регистрации/входа

7. **Валидация**:
   - Проверка роли `if role not in user['role']` не работает корректно, так как `user['role']` - это строка, а не список

8. **API Gateway**:
   - Эндпоинт `/account-delete/{user_id}` недоступен через Gateway (только напрямую)
   - Отсутствует авторизация на уровне Gateway
   - Нет кэширования запросов

## 📡 Интеграция с RabbitMQ

Сервис отправляет события в очередь `user_info` при следующих действиях:

1. **Регистрация пользователя**:
```json
{
  "auth_id": 123,
  "display_name": "John Doe"
}
```

2. **Деактивация своего аккаунта**:
```json
{
  "event_type": "Account Deactivate",
  "is_active": false
}
```

3. **Деактивация чужого аккаунта (Admin)**:
```json
{
  "event_type": "Account Deactivate",
  "user_id": 123,
  "is_active": false
}
```

### Подключение к RabbitMQ

- **URL подключения**: `amqp://guest:guest@rabbitmq:5672/` (⚠️ захардкожен)
- **Веб-интерфейс**: http://localhost:15672 (логин: guest, пароль: guest)
- **Порт AMQP**: 5672
- **Healthcheck**: проверка доступности каждые 30 секунд

## 🗄️ База данных

### Модель UserAuthInfo

```sql
CREATE TABLE auth_info (
    id INTEGER PRIMARY KEY,
    login VARCHAR UNIQUE NOT NULL,
    email VARCHAR UNIQUE NOT NULL,
    hashed_password VARCHAR NOT NULL,
    is_active BOOLEAN DEFAULT TRUE
);
```

### Особенности

- **Асинхронная работа** с базой данных
- **Автоматическое создание таблиц** при старте приложения
- **SQLite** как база данных по умолчанию
- **Уникальные ограничения** на логин и email
- **Soft delete** через поле `is_active` (аккаунт деактивируется, но не удаляется)

> ⚠️ **Ограничение**: При регистрации проверяется уникальность только среди активных пользователей (is_active == True). Деактивированные пользователи не блокируют регистрацию с тем же логином/email.

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `RABBITMQ_HOST` | Хост RabbitMQ | `rabbitmq` (⚠️ не используется) |

> ⚠️ **Примечание**: Большинство настроек захардкожены и не вынесены в переменные окружения.

### JWT настройки

| Параметр | Значение | Хардкод |
|----------|----------|---------|
| Алгоритм | HS256 | ✅ |
| Время жизни access токена | 30 минут | ✅ |
| Время жизни refresh токена | 7 дней | ✅ |
| Секретный ключ | `super-secret` | ✅ |
| Роль по умолчанию | `user` | ✅ |

## 🧪 Тестирование

### Локальное тестирование API

1. **Регистрация пользователя**:
```bash
curl -X POST http://localhost:8080/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "display_name": "John Doe",
    "login": "johndoe",
    "email": "john@example.com",
    "password": "SecurePass123!"
  }'
```

2. **Вход в систему**:
```bash
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "login": "johndoe",
    "password": "SecurePass123!"
  }'
```

3. **Обновление токенов**:
```bash
curl -X POST http://localhost:8080/api/refresh \
  -H "Content-Type: application/json" \
  -d '{
    "refresh_token": "your_refresh_token_here"
  }'
```

4. **Деактивация своего аккаунта**:
```bash
curl -X POST http://localhost:8080/api/account-delete \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer your_access_token_here"
```

5. **Деактивация чужого аккаунта (Admin)**:
```bash
curl -X POST http://localhost:8000/account-delete/123 \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer admin_access_token_here"
```

### Интерактивная документация API

После запуска сервиса доступна интерактивная документация:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 📊 Мониторинг и логи

### Логи сервиса аутентификации

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Логи только auth-service
docker-compose logs -f auth-service

# Логи RabbitMQ
docker-compose logs -f rabbitmq

# Логи Gateway
docker-compose logs -f gateway
```

### Проверка состояния сервисов

```bash
# Статус всех контейнеров
docker-compose ps

# Детальная информация о контейнерах
docker-compose ps -a

# Использование ресурсов
docker stats

# Проверка healthcheck RabbitMQ
docker-compose exec rabbitmq rabbitmq-diagnostics ping
```

## 🚢 Деплой в продакшн

### Критические изменения для продакшн

1. **⚠️ Обязательные**:
   - Измените секретный ключ JWT на случайно сгенерированный
   - Перенесите все хардкоженные настройки в переменные окружения
   - Замените SQLite на PostgreSQL или MySQL
   - Настройте SSL/TLS для всех сервисов
   - Добавьте механизм миграций (Alembic)
   - Реализуйте черный список для токенов

2. **⚠️ Рекомендуемые**:
   - Настройте мониторинг и алерты (Prometheus + Grafana)
   - Используйте балансировщик нагрузки для API Gateway
   - Настройте резервное копирование БД
   - Добавьте rate limiting
   - Реализуйте централизованную обработку ошибок
   - Добавьте детальное логирование
   - Настройте CAPTCHA для защиты от ботов

3. **⚠️ Безопасность**:
   - Используйте переменные окружения для всех секретов
   - Настройте firewall
   - Используйте HTTPS везде
   - Реализуйте защиту от CSRF
   - Добавьте проверку заголовков безопасности

### Пример продакшн docker-compose.yml

```yaml
version: '3.8'
services:
  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
    
  auth-service:
    build: ./auth-service
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      RABBITMQ_URL: ${RABBITMQ_URL}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS}
    depends_on:
      rabbitmq:
        condition: service_healthy
      
  gateway:
    image: devopsfaith/krakend:latest
    ports: 
      - "8080:8080"
    volumes: 
      - ./gateway/krakend.json:/etc/krakend/krakend.json
    depends_on:
      - auth-service
```

### Пример .env файла

```env
# Database
DATABASE_URL=postgresql+asyncpg://user:password@db:5432/auth_db

# JWT
JWT_SECRET=your-super-secret-random-key-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# RabbitMQ
RABBITMQ_URL=amqp://user:password@rabbitmq:5672/
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=secure_password
```

## 🤝 Разработка

### Добавление новых эндпоинтов

1. Создайте функцию в `auth-service/app/crud.py`
2. Добавьте схемы в `auth-service/app/schemas.py`
3. Создайте эндпоинт в `auth-service/app/main.py`
4. Обновите конфигурацию KrakenD в `gateway/krakend.json`

### Структура изменений

При внесении изменений в код:

1. **Модели данных** → `models.py`
2. **Схемы валидации** → `schemas.py`
3. **Бизнес-логика** → `crud.py`
4. **API эндпоинты** → `main.py`
5. **JWT функции** → `jwt.py`
6. **Gateway маршруты** → `gateway/krakend.json`

### TODO: Планируемые улучшения

- [ ] Вынести все настройки в конфигурационный файл (.env)
- [ ] Реализовать полноценную ролевую систему с таблицей ролей
- [ ] Добавить черный список для отозванных токенов
- [ ] Реализовать механизм миграций (Alembic)
- [ ] Добавить rate limiting
- [ ] Реализовать централизованную обработку ошибок
- [ ] Добавить детальное логирование
- [ ] Заменить SQLite на PostgreSQL
- [ ] Добавить индексы в базу данных
- [ ] Реализовать лимит на количество активных сессий
- [ ] Добавить защиту от brute-force атак
- [ ] Настроить CAPTCHA для регистрации/входа
- [ ] Добавить эндпоинт `/account-delete/{user_id}` в Gateway
- [ ] Реализовать систему разрешений (permissions)

## 📞 Поддержка

Для получения помощи или сообщения об ошибках:

1. Проверьте логи сервисов
2. Убедитесь, что все порты доступны
3. Проверьте переменные окружения
4. Обратитесь к документации API
5. Изучите раздел "Известные ограничения и хардкод"

## 📄 Лицензия

Этот проект создан для демонстрационных целей и может быть использован в качестве основы для коммерческих проектов.

---

**Версия документации**: 2.0  
**Последнее обновление**: 2026-02-07  
**Статус проекта**: Development (Alpha)
