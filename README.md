# Auth & User Services - Микросервисы аутентификации и управления пользователями

## Описание проекта

**Auth & User Services** - это система из двух микросервисов, обеспечивающих полноценную аутентификацию и управление пользовательскими данными. Система построена на архитектуре Event-Driven с использованием RabbitMQ для асинхронной коммуникации между сервисами и API Gateway для маршрутизации запросов.

### Компоненты системы:

1. **Auth Service** (порт 8000) - сервис аутентификации с JWT токенами
2. **User Service** (порт 8001) - сервис управления пользовательскими профилями
3. **API Gateway** (KrakenD, порт 8080) - единая точка входа для всех запросов
4. **RabbitMQ** - брокер сообщений для асинхронной коммуникации между сервисами

> ⚠️ **Важно**: Проект находится в стадии разработки и содержит хардкод и известные ограничения, которые планируется устранить в будущих версиях.

## 🏗️ Архитектура

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│                 │         │  Auth Service   │         │                 │
│   API Gateway   │────────▶│   (FastAPI)     │────────▶│   RabbitMQ      │
│   (KrakenD)     │         │   Port: 8000    │         │   Message Queue │
│   Port: 8080    │         │  auth.db        │         │   Port: 5672    │
└─────────────────┘         └─────────────────┘         └─────────────────┘
       │                                                          │
       │                                                          │
       ▼                                                          ▼
┌─────────────────┐                                    ┌─────────────────┐
│  User Service   │◀────────────────────────────────────│   Events        │
│   (FastAPI)     │            (Consumer)               │  (user_info)    │
│   Port: 8001    │                                    └─────────────────┘
│  user.db        │
└─────────────────┘
```

### Потоки данных:

1. **Регистрация пользователя**:
   - Клиент → Gateway → Auth Service (создает пользователя)
   - Auth Service → RabbitMQ (событие Register)
   - RabbitMQ → User Service (создает профиль)

2. **Деактивация аккаунта**:
   - Клиент → Gateway → Auth Service (деактивирует аккаунт)
   - Auth Service → RabbitMQ (событие Account Deactivate)
   - RabbitMQ → User Service (деактивирует профиль)

3. **Получение профиля**:
   - Клиент → Gateway → User Service (проверяет токен → возвращает профиль)

## 🛠️ Технологии

### Backend (Auth Service)
- **Python 3.11** - современная версия Python
- **FastAPI** - высокопроизводительный веб-фреймворк
- **SQLAlchemy (Async)** - асинхронная ORM
- **AIOSQLite** - асинхронный драйвер SQLite
- **JWT (jose)** - библиотека для JSON Web Tokens
- **Passlib[Argon2]** - безопасное хеширование паролей
- **AIO-Pika** - асинхронный клиент для RabbitMQ

### Backend (User Service)
- **Python 3.11** - современная версия Python
- **FastAPI** - высокопроизводительный веб-фреймворк
- **SQLAlchemy (Async)** - асинхронная ORM
- **AIOSQLite** - асинхронный драйвер SQLite
- **AIO-Pika** - асинхронный клиент для RabbitMQ
- **JWT (jose)** - валидация JWT токенов

### Infrastructure
- **Docker & Docker Compose** - контейнеризация
- **KrakenD** - API Gateway
- **RabbitMQ 3** - брокер сообщений

## 📁 Структура проекта

```
auth-service/
├── docker-compose.yml              # Конфигурация Docker Compose
├── README.md                       # Документация проекта
│
├── auth_service/                   # Сервис аутентификации
│   ├── Dockerfile
│   ├── requirements.txt
│   └── app/
│       ├── main.py                 # Главный файл FastAPI
│       ├── models.py               # SQLAlchemy модели (UserAuthInfo, TokenBlackList)
│       ├── schemas.py              # Pydantic схемы
│       ├── crud.py                 # Операции с БД
│       ├── auth.py                 # Хеширование паролей
│       ├── jwt.py                  # JWT токены и аутентификация
│       ├── blacklist.py            # Черный список токенов
│       ├── sender.py               # Отправка событий в RabbitMQ
│       ├── database.py             # Конфигурация БД
│       └── validators.py           # Валидаторы
│
├── user_service/                   # Сервис управления пользователями
│   ├── Dockerfile
│   ├── requirements.txt
│   └── app/
│       ├── main.py                 # Главный файл FastAPI
│       ├── models.py               # SQLAlchemy модели (UserProfile)
│       ├── schemas.py              # Pydantic схемы
│       ├── crud.py                 # Операции с БД
│       ├── consumer.py             # Потребитель событий RabbitMQ
│       ├── jwt_checker.py          # Проверка JWT токенов
│       └── database.py             # Конфигурация БД
│
└── gateway/
    └── krakend.json                # Конфигурация KrakenD Gateway
```

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd auth-service
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Проверьте статус сервисов:
```bash
docker-compose ps
```

### Локальная разработка

1. Установите зависимости для каждого сервиса:
```bash
# Auth Service
cd auth_service
pip install -r requirements.txt

# User Service
cd ../user_service
pip install -r requirements.txt
```

2. Запустите RabbitMQ:
```bash
docker run -d -p 5672:5672 -p 15672:15672 rabbitmq:3-management
```

3. Запустите сервисы:
```bash
# Auth Service (порт 8000)
cd auth_service
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# User Service (порт 8001)
cd user_service
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

## 🔌 API Endpoints

### Через API Gateway (порт 8080)

#### Auth Endpoints

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/register` | Регистрация нового пользователя | ❌ |
| POST | `/api/login` | Вход в систему | ❌ |
| POST | `/api/refresh` | Обновление токенов | ❌ |
| POST | `/api/logout` | Выход из системы (с blacklist) | ✅ (Bearer token) |
| POST | `/api/account-delete` | Деактивация своего аккаунта | ✅ (Bearer token) |
| POST | `/api/account-delete/{user_id}` | Деактивация чужого аккаунта (Admin) | ✅ (Bearer token + role: admin) |

#### User Endpoints

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/profile` | Получить профиль пользователя | ✅ (Bearer token) |
| PATCH | `/api/settings/edit-bio` | Редактировать биографию | ✅ (Bearer token) |

### Прямое обращение к сервисам

#### Auth Service (порт 8000)
- `/register` - Регистрация
- `/login` - Вход
- `/refresh` - Обновление токенов
- `/logout` - Выход
- `/account-delete` - Деактивация своего аккаунта
- `/account-delete/{user_id}` - Деактивация чужого аккаунта (Admin)

#### User Service (порт 8001)
- `/profile` - Получить профиль
- `/edit-bio` - Редактировать биографию

## 📋 Схемы данных

### RegisterSchema
```json
{
  "display_name": "string (3-30 символов, только буквы)",
  "login": "string (3-30 символов, буквы/цифры/_)",
  "email": "string (валидный email)",
  "password": "string (мин. 8 символов: строчные, заглавные, цифры, спецсимволы)"
}
```

### LoginSchema
```json
{
  "login": "string",
  "password": "string"
}
```

### AuthResponseSchema
```json
{
  "user": {
    "id": "integer",
    "login": "string",
    "email": "string",
    "is_active": "boolean"
  },
  "token_pair": {
    "access_token": {
      "token": "string",
      "token_id": "string"
    },
    "refresh_token": {
      "token": "string",
      "token_id": "string"
    }
  }
}
```

### RefreshTokenSchema
```json
{
  "refresh_token": "string"
}
```

### TokenResponseSchema
```json
{
  "access_token": {
    "token": "string",
    "token_id": "string"
  },
  "refresh_token": {
    "token": "string",
    "token_id": "string"
  }
}
```

### UserProfileSchema
```json
{
  "id": "integer",
  "auth_id": "integer",
  "display_name": "string",
  "bio": "string или null",
  "is_active": "boolean"
}
```

### EditBioSchema
```json
{
  "bio": "string (макс. 500 символов)"
}
```

## 🔒 Безопасность и Авторизация

### JWT Токены

#### Access Token
- **Время жизни**: 30 минут
- **Содержит**: `sub` (user_id), `exp` (expiration), `role`, `type: 'access'`, `jti` (token ID), `iat` (issued at)
- **Используется для**: доступа к защищенным эндпоинтам

#### Refresh Token
- **Время жизни**: 7 дней (10080 минут)
- **Содержит**: `sub` (user_id), `exp` (expiration), `role`, `type: 'refresh'`, `jti` (token ID), `iat` (issued at)
- **Используется для**: обновления пары токенов
- **Ротация**: при обновлении старый refresh токен добавляется в blacklist

### Черный список токенов (Blacklist)

Система поддерживает черный список для отозванных токенов:

- **Таблица**: `token_blacklist`
- **Поля**: `jti` (token ID), `user_id`, `expires_at`, `blacklisted_at`
- **Логаут**: добавляет оба токена (access и refresh) в blacklist
- **Refresh**: старый refresh токен добавляется в blacklist
- **Проверка**: каждый токен проверяется на наличие в blacklist

> ✅ **Реализовано**: Черный список токенов работает корректно

### Ролевая модель доступа

- **role**: поле в payload JWT токена
- **required_role()**: зависимость для проверки прав доступа
- **Текущая реализация**: роль 'user' по умолчанию для всех пользователей

> ⚠️ **Ограничение**: Роль захардкожена как 'user' при создании токенов. Отсутствует механизм назначения ролей пользователям.

### Валидация данных

- **Пароль**: минимум 8 символов, должен содержать заглавные и строчные буквы, цифры и спецсимволы (`!@#$%^&*()-=_`)
- **Логин**: 3-30 символов, только буквы, цифры и подчеркивания
- **Имя пользователя**: 3-30 символов, только буквы
- **Email**: обязательная валидация формата
- **Bio**: максимум 500 символов

## 📡 Интеграция с RabbitMQ

### Очередь: `user_info`

Сервисы обмениваются событиями через очередь `user_info`:

#### События, отправляемые Auth Service:

1. **Register** (при регистрации):
```json
{
  "event_type": "Register",
  "auth_id": 123,
  "display_name": "John Doe"
}
```

2. **Account Deactivate** (при деактивации):
```json
{
  "event_type": "Account Deactivate",
  "auth_id": 123,
  "is_active": false
}
```

#### Обработка событий в User Service:

- **Register**: создает новый профиль пользователя в БД
- **Account Deactivate**: деактивирует профиль пользователя

> ⚠️ **Хардкод**: URL подключения к RabbitMQ захардкожен в обоих сервисах

## 🗄️ База данных

### Auth Service (auth.db)

#### UserAuthInfo
```sql
CREATE TABLE auth_info (
    id INTEGER PRIMARY KEY,
    login VARCHAR UNIQUE NOT NULL,
    email VARCHAR UNIQUE NOT NULL,
    hashed_password VARCHAR NOT NULL,
    is_active BOOLEAN DEFAULT TRUE
);
```

#### TokenBlackList
```sql
CREATE TABLE token_blacklist (
    id INTEGER PRIMARY KEY,
    jti VARCHAR NOT NULL,  -- Token ID
    user_id INTEGER NOT NULL,
    expires_at DATETIME NOT NULL,
    blacklisted_at DATETIME NOT NULL
);

CREATE INDEX ix_token_blacklist_jti ON token_blacklist(jti);
```

### User Service (user-service.db)

#### UserProfile
```sql
CREATE TABLE user_profiles (
    id INTEGER PRIMARY KEY,
    auth_id INTEGER UNIQUE NOT NULL,
    display_name VARCHAR NOT NULL,
    bio VARCHAR,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);
```

### Особенности

- **Две отдельные базы данных** для каждого сервиса
- **Асинхронная работа** с базами данных
- **Автоматическое создание таблиц** при старте приложений
- **SQLite** как база данных по умолчанию
- **Soft delete** через поле `is_active`

> ⚠️ **Ограничение**: SQLite не подходит для продакшн с высокой нагрузкой

## 🚨 Известные ограничения и хардкод

### Хардкод в коде

#### Auth Service

1. **Секретный ключ JWT** (`auth_service/app/jwt.py`):
   ```python
   SECRET = 'super-secret'  # ⚠️ Должен быть в переменных окружения
   ```

2. **Роль пользователя** (`auth_service/app/jwt.py`):
   ```python
   payload = {
       'role': 'user',  # ⚠️ Захардкожено, нет механизма назначения ролей
   }
   ```

3. **Роль администратора** (`auth_service/app/main.py`):
   ```python
   admin = Depends(required_role('admin'))  # ⚠️ Роль захардкожена
   ```

4. **URL подключения к RabbitMQ** (`auth_service/app/sender.py`):
   ```python
   rabbit_host = os.getenv('RABBITMQ_HOST', 'rabbitmq')  # ⚠️ Default захардкожен
   return await aio_pika.connect_robust(f'amqp://guest:guest@{rabbit_host}')
   ```

5. **Алгоритм JWT** (`auth_service/app/jwt.py`):
   ```python
   ALGORITHM = 'HS256'  # ⚠️ Может быть вынесен в конфиг
   ```

6. **Время жизни токенов** (`auth_service/app/jwt.py`):
   ```python
   ACCESS_TOKEN_EXPIRE_MINUTES = 30  # ⚠️ Захардкожено
   REFRESH_TOKEN_EXPIRE_MINUTES = 10080  # ⚠️ Захардкожено
   ```

#### User Service

1. **Секретный ключ JWT** (`user_service/app/jwt_checker.py`):
   ```python
   SECRET = 'super-secret'  # ⚠️ Должен быть в переменных окружения
   ```

2. **Алгоритм JWT** (`user_service/app/jwt_checker.py`):
   ```python
   ALGORITHM = 'HS256'  # ⚠️ Может быть вынесен в конфиг�
   ```

3. **URL подключения к RabbitMQ** (`user_service/app/consumer.py`):
   ```python
   rabbit_host = os.getenv('RABBITMQ_HOST', 'rabbitmq')  # ⚠️ Default захардкожен
   return await aio_pika.connect_robust(f'amqp://guest:guest@{rabbit_host}')
   ```

4. **Путь к БД** (`user_service/app/database.py`):
   ```python
   SQLALCHEMY_DATABASE_URL = "sqlite+aiosqlite:///./user-service.db"  # ⚠️ Захардкожен
   ```

### Архитектурные ограничения

1. **Ролевая система**:
   - Отсутствует механизм назначения ролей пользователям
   - Роль захардкожена как 'user' при создании токенов
   - Проверка роли работает только для одной роли (не поддерживает список ролей)
   - Нет таблицы ролей в базе данных
   - Нет связи между сервисами для синхронизации ролей

2. **Конфигурация**:
   - Отсутствует файл конфигурации (.env)
   - Большинство настроек захардкожены в коде
   - Нет разделения конфигураций для dev/prod
   - Секретные ключи одинаковые в обоих сервисах (⚠️ security issue)

3. **База данных**:
   - SQLite не подходит для продакшн с высокой нагрузкой
   - Отсутствует механизм миграций (Alembic)
   - Нет индексов для оптимизации запросов (кроме blacklist.jti)
   - Две отдельные базы данных без явной синхронизации

4. **Обработка ошибок**:
   - Отсутствует централизованная обработка исключений
   - Нет детального логирования ошибок
   - Часть ошибок возвращает статус 401 вместо более подходящих кодов

5. **Безопасность**:
   - Отсутствует rate limiting
   - Нет защиты от brute-force атак
   - Отсутствует логирование неудачных попыток входа
   - Нет CAPTCHA для регистрации/входа
   - Одинаковые секретные ключи в обоих сервисах

6. **Коммуникация сервисов**:
   - RabbitMQ без подтверждения доставки сообщений
   - Нет механизма повторной отправки при ошибках
   - Нет dead-letter queue для неудачных сообщений
   - Отсутствует валидация событий

7. **API Gateway**:
   - Отсутствует авторизация на уровне Gateway
   - Нет кэширования запросов
   - Отсутствует ограничение частоты запросов

8. **Blacklist**:
   - Нет автоматической очистки устаревших токенов
   - Нет лимита на количество токенов в blacklist
   - Возможна утечка памяти при большом количестве токенов

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию | Используется |
|------------|----------|----------------------|--------------|
| `RABBITMQ_HOST` | Хост RabbitMQ | `rabbitmq` | auth_service, user_service |

> ⚠️ **Примечание**: Большинство настроек захардкожены и не вынесены в переменные окружения.

### JWT настройки

| Параметр | Значение | Хардкод |
|----------|----------|---------|
| Алгоритм | HS256 | ✅ |
| Время жизни access токена | 30 минут | ✅ |
| Время жизни refresh токена | 7 дней | ✅ |
| Секретный ключ | `super-secret` | ✅ |
| Роль по умолчанию | `user` | ✅ |

## 🧪 Тестирование

### Локальное тестирование API

1. **Регистрация пользователя**:
```bash
curl -X POST http://localhost:8080/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "display_name": "John Doe",
    "login": "johndoe",
    "email": "john@example.com",
    "password": "SecurePass123!"
  }'
```

2. **Вход в систему**:
```bash
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "login": "johndoe",
    "password": "SecurePass123!"
  }'
```

3. **Получить профиль**:
```bash
curl -X GET http://localhost:8080/api/profile \
  -H "Authorization: Bearer your_access_token_here"
```

4. **Редактировать биографию**:
```bash
curl -X PATCH http://localhost:8080/api/settings/edit-bio \
  -H "Authorization: Bearer your_access_token_here" \
  -H "Content-Type: application/json" \
  -d '{
    "bio": "Hello, I am John!"
  }'
```

5. **Обновление токенов**:
```bash
curl -X POST http://localhost:8080/api/refresh \
  -H "Content-Type: application/json" \
  -d '{
    "refresh_token": "your_refresh_token_here"
  }'
```

6. **Логаут**:
```bash
curl -X POST http://localhost:8080/api/logout \
  -H "Authorization: Bearer your_access_token_here" \
  -H "Content-Type: application/json" \
  -d '{
    "refresh_token": "your_refresh_token_here"
  }'
```

7. **Деактивация своего аккаунта**:
```bash
curl -X POST http://localhost:8080/api/account-delete \
  -H "Authorization: Bearer your_access_token_here"
```

### Интерактивная документация API

- **Auth Service**: http://localhost:8000/docs
- **User Service**: http://localhost:8001/docs
- **ReDoc (Auth)**: http://localhost:8000/redoc
- **ReDoc (User)**: http://localhost:8001/redoc

## 📊 Мониторинг и логи

### Логи сервисов

```bash
# Все сервисы
docker-compose logs -f

# Auth Service
docker-compose logs -f auth-service

# User Service
docker-compose logs -f user-service

# RabbitMQ
docker-compose logs -f rabbitmq

# Gateway
docker-compose logs -f gateway
```

### Проверка состояния

```bash
# Статус всех контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# RabbitMQ UI
http://localhost:15672 (guest/guest)
```

## 🚢 Деплой в продакшн

### Критические изменения для продакшн

1. **⚠️ Обязательные**:
   - Измените секретные ключи JWT на случайно сгенерированные
   - Используйте разные секретные ключи для каждого сервиса
   - Перенесите все хардкоженные настройки в переменные окружения
   - Замените SQLite на PostgreSQL или MySQL
   - Настройте SSL/TLS для всех сервисов
   - Добавьте механизм миграций (Alembic)
   - Настройте автоматическую очистку blacklist

2. **⚠️ Рекомендуемые**:
   - Настройте мониторинг и алерты (Prometheus + Grafana)
   - Используйте балансировщик нагрузки для API Gateway
   - Настройте резервное копирование БД
   - Добавьте rate limiting
   - Реализуйте централизованную обработку ошибок
   - Добавьте детальное логирование
   - Настройте CAPTCHA для защиты от ботов
   - Добавьте dead-letter queue для RabbitMQ

3. **⚠️ Безопасность**:
   - Используйте переменные окружения для всех секретов
   - Настройте firewall
   - Используйте HTTPS везде
   - Реализуйте защиту от CSRF
   - Добавьте проверку заголовков безопасности

### Пример продакшн docker-compose.yml

```yaml
version: '3.8'
services:
  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "5672:5672"
      - "15672:15672"
    
  auth-service:
    build: ./auth_service
    environment:
      DATABASE_URL: ${AUTH_DATABASE_URL}
      JWT_SECRET: ${AUTH_JWT_SECRET}
      RABBITMQ_URL: ${RABBITMQ_URL}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS}
    depends_on:
      - rabbitmq
      
  user-service:
    build: ./user_service
    environment:
      DATABASE_URL: ${USER_DATABASE_URL}
      JWT_SECRET: ${USER_JWT_SECRET}
      RABBITMQ_URL: ${RABBITMQ_URL}
    depends_on:
      - rabbitmq
      
  gateway:
    image: devopsfaith/krakend:latest
    ports: 
      - "8080:8080"
    volumes: 
      - ./gateway/krakend.json:/etc/krakend/krakend.json
    depends_on:
      - auth-service
      - user-service
```

### Пример .env файла

```env
# Database
AUTH_DATABASE_URL=postgresql+asyncpg://user:password@auth-db:5432/auth_db
USER_DATABASE_URL=postgresql+asyncpg://user:password@user-db:5432/user_db

# JWT (разные ключи для разных сервисов!)
AUTH_JWT_SECRET=your-auth-super-secret-key-min-32-chars
USER_JWT_SECRET=your-user-super-secret-key-min-32-chars

# JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# RabbitMQ
RABBITMQ_URL=amqp://user:password@rabbitmq:5672/
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=secure_password
```

## 🤝 Разработка

### Добавление новых эндпоинтов

1. Создайте функцию в `crud.py`
2. Добавьте схемы в `schemas.py`
3. Создайте эндпоинт в `main.py`
4. Обновите конфигурацию KrakenD в `gateway/krakend.json`

### Добавление новых событий

1. Отправьте событие через `send_event()` в Auth Service
2. Добавьте обработчик в `consumer.py` в User Service
3. Обновите документацию

### TODO: Планируемые улучшения

- [ ] Вынести все настройки в конфигурационный файл (.env)
- [ ] Использовать разные секретные ключи для каждого сервиса
- [ ] Реализовать полноценную ролевую систему с таблицей ролей
- [ ] Добавить автоматическую очистку blacklist токенов
- [ ] Реализовать механизм миграций (Alembic)
- [ ] Добавить rate limiting
- [ ] Реализовать централизованную обработку ошибок
- [ ] Добавить детальное логирование
- [ ] Заменить SQLite на PostgreSQL
- [ ] Добавить индексы в базу данных
- [ ] Добавить защиту от brute-force атак
- [ ] Настроить CAPTCHA для регистрации/входа
- [ ] Добавить dead-letter queue для RabbitMQ
- [ ] Реализовать систему разрешений (permissions)
- [ ] Добавить валидацию событий между сервисами

## 📞 Поддержка

Для получения помощи или сообщения об ошибках:

1. Проверьте логи всех сервисов
2. Убедитесь, что RabbitMQ доступен
3. Проверьте переменные окружения
4. Обратитесь к документации API
5. Изучите раздел "Известные ограничения и хардкод"

## 📄 Лицензия

Этот проект создан для демонстрационных целей и может быть использован в качестве основы для коммерческих проектов.

---

**Версия документации**: 2.0  
**Последнее обновление**: 2026-02-07  
**Статус проекта**: Development (Alpha)
